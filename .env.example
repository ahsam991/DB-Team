# Copy to .env.local and fill in real values, or set these in Vercel → Project → Environment Variables

SUPABASE_URL=https://hgipkotrchoksdpuotjt.supabase.co
SUPABASE_ANON_KEY=sb_publishable_T9XHNPPcy6eAS8xPH1kG2w_Shfmb_Z3
# Secret/service-role key (server-side only) — from Supabase → Settings → API → Secret keys
SUPABASE_SERVICE_ROLE_KEY=your-secret-key-here
# Comma-separated emails allowed to access the admin panel
ADMIN_EMAILS=admin@example.com